// Boot entry. Step 1 scaffold: load global styles so the empty dark page paints.
// Full boot sequence (settings load, module mount) lands in step 2.
import './styles/tokens.css';
import './styles/fonts.css';
import './styles/base.css';
